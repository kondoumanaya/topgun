#!/bin/bash -eux

# Run type checking with mypy.

# Install dependencies and topgun in editable mode
uv pip install -r requirements/typing.txt
uv pip install -e ./topgun

# Run mypy using the environment where packages are installed
python -m mypy bots/
