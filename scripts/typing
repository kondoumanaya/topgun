#!/bin/bash -eux

# Run type checking with mypy.

# Install dependencies and topgun in editable mode (use --system for CI)
uv pip install --system -r requirements/typing.txt
uv pip install --system -e ./topgun

# Run mypy using the environment where packages are installed
python -m mypy bots/
