# GMO Board Watcher Bot Dependencies
aiohttp>=3.7.0
typing-extensions>=4.0.0
python-dotenv>=1.0.0

rich>=13.0.0

# Install topgun as editable dependency
# Run: pip install -e ../../topgun

# Shared modules are imported from parent project